{% import "validity.md.j2" as validity_macros %}
{% macro value(value, precision=2) -%}
{% if value is none -%}
-
{%- else -%}
{{ ("%%.%df" % precision)|format(value) }}
{%- endif %}
{%- endmacro %}
# FogHTTP response streaming benchmark results

- Timestamp: `{{ timestamp }}`
- Python: `{{ python_version }}`
- Platform: `{{ platform_name }}`
- Server: `local asyncio HTTP/1.1 loopback server`
- Streams/run: `{{ args.requests }}`
- Warmup/run: `{{ args.warmup }}`
- Repeats: `{{ args.repeats }}`
- Shuffle seed: `{{ args.seed }}`

{% if skipped %}
## Skipped clients

{% for name, reason in skipped.items() %}
- `{{ name }}`: {{ reason }}
{% endfor %}

{% endif %}
{{ validity_macros.summary(validity) }}

## Aggregate

| mode | client | case | read | consume | conc | streams/s median | MiB/s median | lines/s median | cv % | p50 ms | p95 ms | p99 ms | first item p95 ms | errors % | max RSS MB | max threads | max fds | body closed | body reuse | body aborted | conn opened | conn reused | conn closed | conn aborted | idle conn | error types |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
{% for row in aggregate %}
| {{ row.mode }} | {{ row.client }} | {{ row.case }} | {{ row.read }} | {{ row.consume }} | {{ row.concurrency }} | {{ value(row.ok_streams_s_median, 1) }} | {{ value(row.mb_s_median, 1) }} | {{ value(row.lines_s_median, 1) }} | {{ value(row.streams_s_cv_percent, 1) }} | {{ value(row.p50_ms_median, 2) }} | {{ value(row.p95_ms_median, 2) }} | {{ value(row.p99_ms_median, 2) }} | {{ value(row.first_chunk_p95_ms_median, 2) }} | {{ value(row.error_rate_percent, 2) }} | {{ value(row.rss_mb_max, 1) }} | {{ row.threads_max }} | {{ row.fds_max }} | {{ row.final_response_body_closed_max if row.final_response_body_closed_max is not none else "-" }} | {{ row.final_response_body_reuse_eligible_max if row.final_response_body_reuse_eligible_max is not none else "-" }} | {{ row.final_response_body_aborted_max if row.final_response_body_aborted_max is not none else "-" }} | {{ row.final_connections_opened_max if row.final_connections_opened_max is not none else "-" }} | {{ row.final_connections_reused_max if row.final_connections_reused_max is not none else "-" }} | {{ row.final_connections_closed_max if row.final_connections_closed_max is not none else "-" }} | {{ row.final_connections_aborted_max if row.final_connections_aborted_max is not none else "-" }} | {{ row.final_idle_connections_max if row.final_idle_connections_max is not none else "-" }} | `{{ row.error_types }}` |
{% endfor %}
